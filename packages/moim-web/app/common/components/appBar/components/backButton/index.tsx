import styled from "styled-components";
// icons
import BackIcon from "@icon/24-back-b";

const BackButton = styled(BackIcon).attrs({
  role: "button",
  size: "s",
  touch: 44,
})``;

export default BackButton;
