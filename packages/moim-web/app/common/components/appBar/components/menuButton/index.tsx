import styled from "styled-components";
// icons
import MenuIcon from "@icon/24-menu-g.svg";

const MenuButton = styled(MenuIcon).attrs({
  role: "button",
  size: "s",
  touch: 44,
})``;

export default MenuButton;
