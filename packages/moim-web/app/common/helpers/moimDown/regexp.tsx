export const SERVER_SPEC_MENTION = /<(@|#)([^>:]+?)>/g;
export const RENDER_SPEC_MENTION = /<(@|#)([^>]+?):([^>]+?)>/g;
export const HIGHLIGHTED_TEXT_REGEX = /<(highlighted) ([^>:]+?)>/g;
export const COMMON_REGEX = /<([^>:]+?)\|([^>:]+?)\|([^>:]+?)>/g;
